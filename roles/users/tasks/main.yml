---
# - name: Смотрим сколько пользователей
#   find:
#    paths: /users
#    file_type: directory
#   register: users
# Психанул
- name: vars
  include_vars:
   file: /etc/ansible/git/vars/vars.yaml
- import_tasks: create.yml
# - import_tasks: root.yml
# - import_tasks: copy.yml
